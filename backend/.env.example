# Secret key — change this before deploying to production!
SECRET_KEY=rehab-secret-key-change-in-production

# JWT settings
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Backend server
HOST=0.0.0.0
PORT=8000

# Database path (relative to backend/)
DB_PATH=rehab_data.db
